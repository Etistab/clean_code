{
  "name": "clean_code",
  "version": "1.0.0",
  "description": "Clean code KATA",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx ts-node src/index.ts",
    "build": "npx tsc",
    "test": "npx jest --testTimeout=10000"
  },
  "jest": {
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ],
    "transform": {
      "\\.ts$": "ts-jest"
    }
  },
  "author": "Etistab",
  "license": "ISC",
  "engines": {
    "node": ">=14.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^7.0.0",
    "@types/cucumber": "^7.0.0",
    "cucumber-tsflow": "^4.0.0-rc.1",
    "jest": "^26.6.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@types/jest": "^26.0.20",
    "nedb": "^1.8.0",
    "ts-jest": "^26.5.0"
  }
}
